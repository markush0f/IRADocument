"""Docker configuration patterns."""

DOCKER_FILES = {
    "Dockerfile",
}

DOCKER_COMPOSE_FILES = {
    "docker-compose.yml",
    "docker-compose.yaml",
    "compose.yml",
    "compose.yaml",
}

DOCKER_IGNORE_FILES = {
    ".dockerignore",
}
